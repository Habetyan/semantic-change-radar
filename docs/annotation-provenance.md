# Annotation provenance and author review

The project author reported reviewing the displayed cases and confirmed the
published measurements on 2026-09-24. This records the author's confirmation.
No exported passage-level corrections were incorporated, so the versioned
annotations, predictions, and numerical results remain unchanged. This is not
a new independent adjudication of every benchmark label.

The initial reference annotations were drafted with AI assistance and checked
by separate AI agents where the original protocols record that step. The
32 pilot document pairs and five illustrative demo scenarios are synthetic;
the real-document corpora retain authentic, pinned upstream revisions.
The separate Qwen explanation audit also used AI-assisted judgments.

Scores measure agreement with those frozen reference annotations. Source
authenticity and successful execution do not by themselves establish label
correctness or general accuracy. Corpus sizes, failure cases, and evaluation
limits remain in the [results](resume-results.md).

Historical manifests, review-status fields, freeze files, and experiment plans
retain the status recorded when they were created. They are preserved for
reproducibility; this note records the subsequent author confirmation. Further
passage-level corrections should create a new annotation version and a new run.
